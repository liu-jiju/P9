{
  // include指定哪些路径的文件需要编译，可以写多
  //  **目录    *文件
  "include": ["./src/**/*"],
  // 不包含那些文件，不被编译
  // 默认值：["node_moduls","bower_components","jspm_packages"]
  // 可选
  // "exclude": ["./src/hello/*"]
  // 包含关系，包含数组里面的文件被编译
  // "files": ["index.ts","demo.ts"],
  "compilerOptions": {
    //严格模式的开启
    "strict": true,
    //  'es3', 'es5', 'es6', 'es2015', 'es2016', 'es2017', 
    // 'es2018', 'es2019', 'es2020', 'es2021', 'es2022', 'esnext'.
    "target": "es6",
    // 编译后文件存放的位置
    "outDir": "./dist",
    // 将所有的文件输出到一个文件中进行编译
    "outFile": "./dist/independent/independent.js",
    //是否移除编译后的注释
    "removeComments": true,
    // 指定项目中用到那些库
    // "lib": ["xdxx"]

    // 指定要使用的模块化规范
    "module": "XXX"
  }
}
